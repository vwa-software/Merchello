@inherits Umbraco.Web.Macros.PartialViewMacroPage







@*@inherits Umbraco.Web.Macros.PartialViewMacroPage
    @inherits Merchello.Web.Mvc.MerchelloTemplatePage

    @using Merchello.Core
    @using Merchello.Core.Models
    @using Merchello.FastTrack.Ui
    @using Merchello.Web
    @{
        var storeRoot = ExampleUiHelper.Content.GetStoreRoot();
        var catalogRoot = storeRoot.FirstChild(x => x.DocumentTypeAlias == "ftProduct" && x.IsVisible());
    }

    @foreach (var package in Model.GetPropertyValue<IEnumerable<IPublishedContent>>("featuredProducts"))
    {
        @package.Id
    }



        @{ Model.GetPropertyValue<IEnumberable<IProductContent>>("productsMultiPicker")

*@ }

